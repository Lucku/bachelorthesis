enclave {
    from "sgx_tstdc.edl" import *;

    trusted {
		public int enclaveIterate([in, count=length] uint64_t *data, size_t length);
        public int enclaveVByteEncode([in, count=inLength] uint32_t *in, size_t inLength, [out, count=outLength] uint8_t *out, size_t outLength);
		public int enclaveVByteDecode([in, count=inLength] uint8_t *in, size_t inLength, [out, count=outLength] uint32_t *out, size_t outLength);
    };

    untrusted {
    };
};
