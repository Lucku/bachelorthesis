enclave {
    from "sgx_tstdc.edl" import *;

    trusted {
		public void enclaveIterate([in, size=length] uint64_t *data, size_t length);
        public void enclaveVByteEncode([in, size=inLength] uint32_t *in, size_t inLength, [out, size=outLength] uint8_t *out, size_t outLength);
		public void enclaveVByteDecode([in, size=inLength] uint8_t *in, size_t inLength, [out, size=outLength] uint32_t *out, size_t outLength);
    };

    untrusted {
    };
};
