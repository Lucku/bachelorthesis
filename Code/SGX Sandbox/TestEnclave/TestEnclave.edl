enclave {
    trusted {
        /* define ECALLs here. */
				
		public void enclaveSaveString([in, size=len] char* input, size_t len);
		public void enclaveLoadString([out, size=len] char* output, size_t len);
		public void enclaveSaveInt(int input);
		public int enclaveLoadInt();
    };

    untrusted {
        /* define OCALLs here. */
    
    };
};
